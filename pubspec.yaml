name: krishisource
description: "KrishiSource - Agricultural intelligence app."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.2
  # isar_community is the actively-maintained drop-in replacement for isar v3,
  # fully compatible with modern Flutter/Dart and flutter_riverpod v3.
  http: ^1.6.0
  isar_community: ^3.3.1
  isar_community_flutter_libs: ^3.3.1
  path_provider: ^2.1.5
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.8
  isar_community_generator: ^3.3.1

flutter:
  uses-material-design: true


